<UserControl x:Class="LagoVista.GCode.Sender.Application.Controls.CurrentFileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left"  >
                <Label Content="{Binding Path=Machine.CurrentJob.CurrentIndex, FallbackValue=-}" Margin="0,0,-6,0"/>
                <Label Content="/"/>
                <Label Content="{Binding Path=Machine.CurrentJob.TotalLines, FallbackValue=-}" Margin="-8,0,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Label Content="{Binding Path=Machine.CurrentJob.EstimatedTime.Minutes, FallbackValue=0}"  />
                <Label Content=":" />
                <Label Content="{Binding Path=Machine.CurrentJob.EstimatedTime.Seconds, FallbackValue=00, StringFormat=00}"  />
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1" Name="ListViewFile"  ItemsSource="{Binding Path=Machine.CurrentJob.Commands}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="50" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding LineNumber}" />
                        <TextBlock Text="{Binding Line}" Grid.Column="1" />
                        <TextBlock Text="{Binding EstimatedRunTime.TotalSeconds}" Grid.Column="2" />
                        <TextBlock Text="{Binding Feed}" Grid.Column="2" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
